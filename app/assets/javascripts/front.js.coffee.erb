$ ->
  $('#load-more-docs')
    .on 'ajax:beforeSend', ->
      $(this).html('<img src="<%= image_url "loader.gif" %>" />')
      $(this).attr('disabled', 'disabled')
      $(this).addClass('btn-disabled')
      $(this).attr('href', '#')
